Check the unit tests some of them are failing but shouldn't. 

Add tests to the unit tests which ensure that the parser returns `right` values.

Add more unit tests.

Add tests for invalid input.

Add tests to check the individual parsers too.

Remove Iso8601/ and Iso8601.hs

Think about UnixTimestamp again. In particular anniversaries and leap years. Find the documentation of date -> unix timestamp
